\documentclass[conference]{IEEEtran}
\usepackage[utf8]{inputenc}

\usepackage{graphicx}
\usepackage{caption}
\usepackage{amsmath,amssymb}
\usepackage{multirow}
\usepackage{hyperref}
\usepackage{float}  
\usepackage{booktabs}
\usepackage{siunitx}
\usepackage{url}
% Table wrapping and better column control
\usepackage{tabularx}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{makecell}
% Better verbatim with line breaking
\usepackage{fvextra}
% Slightly increase default cell padding in tables
\setlength{\tabcolsep}{7pt}
% Ragged-right X column that allows wrapping
\newcolumntype{Y}{>{\RaggedRight\arraybackslash}X}

\begin{document}

\title{Knowledge-and-Language 2025/2026 \\
{\textsuperscript{Project Report}}\\
A Bilingual Recipe Assistant Combining a Knowledge Graph, Lightweight NLP, and LLMs
}

\author{
\IEEEauthorblockN{Miguel Castela, uc2022212972}
\IEEEauthorblockA{\textit{DEI, Universidade de Coimbra} \\
uc2022212972@student.uc.pt}
\and
\IEEEauthorblockN{Miguel Martins, uc2022213951}
\IEEEauthorblockA{\textit{DEI, Universidade de Coimbra} \\
uc2022213951@student.uc.pt}
}

\maketitle

% -------- ABSTRACT ----------
\section{Abstract}
\noindent
We present a bilingual recipe assistant that combines a curated recipe Knowledge Graph (KG), intent classification, entity extraction and SPARQL query retrieval.
This ends in an LLM generating the summary of the results in the user's language. In this report, we describe the data curation, approach, implementation, and experiments, highlighting strengths, limitations, and future directions.

% -------- INTRODUCTION ----------
\section{Introduction}

\noindent
\textbf{Motivation.}
\\
Recipe search is often unstructured and specific. Users can query for ingredients, dietary tags, or time constraints. Most systems either rely on pure keyword search or naive large language model answers. We aim to combine structured knowledge graph retrieval with natural language generation, ensuring factual retrieval and  usability.

\noindent
\\
\textbf{Problem Description.}
Given a user query in Portuguese or English, the system must:
\begin{itemize}
  \item Detect intent;
  \item Extract and link entities to the KG via fuzzy matching;
  \item Retrieve candidate results via SPARQL;
  \item Perform a RAG similarity search over recipe descriptions;   
  \item Generate summaries based on all the information, the user's query and the recent history;
  \item Present results through an interactive interface.
\end{itemize}



\noindent
\\
\textbf{Goal and Approach}
Our approach is building the following pipeline:



\begin{itemize}
  \item A curated recipe knowledge graph in RDF/Turtle;
  \item An intent classifier trained with BERT;
  \item An NLP pipeline for entity extraction and fuzzy linking;
  \item A FAISS embedding index for recipe descriptions;
  \item LLM-based summarization;
  \item A React/Vite-based frontend.
\end{itemize}


% -------- RELATED WORK ----------

\section{Related Work}

\noindent
\\
Research on integrating symbolic knowledge representations with neural language models has expanded significantly in recent years, particularly in the context of improving factual accuracy and domain-specific reasoning. A central issue identified by Agrawal et al.~\cite{agrawal2024kg_hallucinations} is that Large Language Models (LLMs) often struggle with domain grounding due to limitations in context comprehension, factual knowledge, and reliable recall. This has motivated work on hybrid systems that augment neural architectures with structured knowledge resources such as RDF/OWL Knowledge Graphs (KGs) and external retrieval modules. For this effect RDF encodes information as subject–predicate–object triples, enabling flexible graph-based modelling \cite{KL2025slides}. RDFS adds schema semantics such as class hierarchies and domain/range constraints, while OWL introduces richer logical constructs including cardinality restrictions, equivalence, and disjointness \cite{KL2025slides}.

\noindent
\\
In these hybrid systems, SPARQL querying allows precise retrieval of RDF data \cite{KL2025slides}, while either NLP rule based systems or more complex tools such as spaCy \cite{honnibal2020spacy} and BERT \cite{devlin2019bert} enable entity recognition, intent classification, and mapping between natural language and the KG entities. 


\noindent
\\
A major line of research addressing LLM factuality is Retrieval-Augmented Generation (RAG). Lewis et al.~\cite{lewis2020rag} introduced RAG as a hybrid architecture combining a parametric generator (BART \cite{lewis2019bart}) with non-parametric memory built from Dense Passage Retrieval (DPR) \cite{karpukhin2020dense_passage_retrieval}. The RAG-Sequence and RAG-Token variants further refine retrieval–generation interactions, with the latter enabling token-level grounding from different retrieved documents \cite{lewis2020rag}. This framework has become a widely adopted strategy for mitigating hallucinations in neural generation.


\noindent
\\
After researching various approaches to integrating knowledge graphs with LLMs for domain-specific question answering, we identified RecipeRAG \cite{loesch2025reciperag} as a particularly relevant prior work for our project. RecipeRAG addresses the challenge of answering complex recipe queries by combining structured knowledge graph embeddings with generative modelling. KGEs such as TransE \cite{bordes2013translating}, RotatE \cite{sun2019rotate}, and QuatE \cite{zhang2019quaternion} encode entities and relations into continuous vector spaces, enabling similarity-based retrieval that respects graph structure. RecipeRAG demonstrates that KGE-driven retrieval outperforms purely text-based similarity when handling multi-criteria recipe search, and further integrates retrieved KG evidence into a generative model to produce personalized recipes. This work highlights the potential of combining symbolic knowledge, embedding-based retrieval, and generative modelling in domain-specific applications such as recipe recommendation.


\noindent
\\
Given this context, the project proposed in this report builds on these research directions by integrating (i) an RDFS-based KG, (ii) an NLP pipeline using BERT \cite{devlin2019bert} and spaCy \cite{honnibal2020spacy} for intent and entity extraction, and (iii) a retrieval step inspired by RAG \cite{lewis2020rag}, enriched with structured recipe data. Similar to RecipeRAG \cite{loesch2025reciperag}. Instead of relying solely on knowledge graph embeddings for retrieval, our approach combines precise SPARQL querying with an embedding similarity search over recipe descriptions.
This hybrid architecture aims to leverage the strengths of structured knowledge representation, lightweight NLP techniques, and LLM generation to provide more accurate, context-aware answers to bilingual recipe queries based on both structured and unstructured data.


% -------- DATA & APPROACH ----------
\section{Data Selection}



\noindent

\noindent
Firstly we searched for a dataset with portuguese recipes, but the ones we found were poorly structured.
(\cite{portuguese_meals_kaggle}) is a portuguese cuisine dataset that unfortunately did not meet our requirements for structure and completeness.

\noindent
Thus, we opted to use a portion of the Food.com \cite{foodcom_kaggle} dataset that, in whole, contains over 200,000 recipes, 

\noindent
The dataset was already curated beacuse its based on the website data that contain only recipes with the following information:

\begin{table}[h!]
\centering
\caption{Fields present in each recipe entry.}
\label{tab:recipe-fields}
% Add explicit spacing between columns (1em) and keep outer margins tight
\begin{tabularx}{\columnwidth}{@{} l @{\hspace{1.5em}} Y @{}}
% Left-align headers to match column alignment
\toprule
\textbf{Field} & \textbf{Description} \\
\midrule
\texttt{name} & Recipe name \\
\texttt{id} & Unique recipe identifier \\
\texttt{minutes} & Total preparation/cooking time \\
\texttt{contributor\_id} & ID of the user who submitted the recipe \\
\texttt{submitted} & Submission date \\
\texttt{tags} & List of categorical tags \\
\texttt{nutrition} & Nutritional information vector \\
\texttt{n\_steps} & Number of preparation steps \\
\texttt{steps} & Ordered list of preparation steps \\
\texttt{description} & Short user's recipe description \\
\texttt{ingredients} & List of ingredients \\
\texttt{n\_ingredients} & Number of ingredients \\
\bottomrule
\end{tabularx}
\end{table}


\noindent
However, as stated in the project proposal, we aim to have a bilingual dataset containing recipes in either portuguese or english languages. This is necessary to then be able to make acurate similarity retrievals and SPARQL queries based on user queries in either of these two languages.


\noindent
This filtering was achieved using two methods 
\begin{itemize}
  \item Firstly, some portuguese recipes were automatically matched using a set of specific keywords defined by hand (for example: "bacalhau", "lisbon", "azores") using regular expressions. In this stage, a pre-trained langid model was also used (ENCOMENDA REAL) to classify portuguese recipes by their full scentences, with a probability score of above 0.8. In this stage
  \item A lot of false negatives were still present from the first step. A pre-trained fasttext LLM (lid.176.bin) (ENCOMENDA REAL) was used to predict the language of each token from the data of each recipe. To be labeled as portuguese a token would have to be considered as 'PT' with a confidence > 0.7 and the difference to the second detected language had to be >0.15. 
\end{itemize} 

\noindent
\\
Both datasets were merged, however there was stil a relatively high rate of false positives, a total of around 1435. This was partially due to a lot of spanish recipes being classified as portuguese on account of the similarity of the two languages. This problem was solved with a pass of the merged dataset through an LLM (chatGPT) (ENCOMENDA REAL) to classify the recipes as portuguese or not portuguese.

\noindent
\\
After removing the false positives 1015 portuguese recipes remained. After this around 9000 random recipes were added to the dataset. To keep portuguese and english recipes (VER O METODO DE TIRAR RECEITAS QUE N SEJAM INGLES)

\section{Approach}

\subsection{Knowledge Graph Construction}
\noindent
After the final dataset of around 10,000 recipes was obtained, the knowledge graph was gemerated io RDFS format. For the graph generation only the following fields were used as RDFS classes: name, id, minutes, tags, nutrition, n\_steps, steps, ingredients, n\_ingredients. This was the decided structured considered most relevant to build the proposed chatbot.

\subsection{Intent Training}

\noindent
The first step to analise the user's query is to detect its intent. A list of five possible intents was defined, and a dataset of around 1,100 training examples was generated using seed samples and an LLM. This dataset contained both portuguese and english examples. The following table shows some examples of user queries and their associated intents:


\begin{table}[h!]
\centering
\caption{Examples of user queries and their associated intents.}
\label{tab:intent-examples}
\begin{tabularx}{\columnwidth}{@{} l @{\hspace{1.5em}} Y @{}}
\toprule
\textbf{User Query} & \textbf{Intent} \\
\midrule
Quanto tempo para cozer peito de frango & \texttt{get\_prep\_time} \\
Find low-carb meals & \texttt{list\_by\_tag} \\
What do I need for paella & \texttt{retrieve\_ingredients} \\
Como fazer francesinha em casa & \texttt{find\_recipe} \\
Pratos para cozinhar em 45 minutos & \texttt{list\_by\_time} \\
\bottomrule
\end{tabularx}
\end{table}

In the dataset building, it was necessary to ensure a clear separation between similar cases such as ``recipes that take 30 minutes'' (\texttt{list\_by\_time}) and ``how long does arroz de pato take to cook?'' (\texttt{get\_prep\_time}).


\subsection{Entity Extraction}
\noindent
The next task is to extract relevant entities from the user query in order to have a base to build the SPARQL queries. This is done using a similarity search with fuzzy matching against the knowledge graph entities. The entites are extracted according to the predicted intent that has the most confidence. The entities that are considered and thus passed to the pipeline are ordered according to their similarity score, if above a certain threshold. The following table shows the mapping of intents to the entities extracted by the pipeline:

\begin{table}[h!]
\centering
\caption{Mapping of intents to the entities extracted by the pipeline.}
\label{tab:intent-to-slots}
\begin{tabularx}{\columnwidth}{@{} l @{\hspace{1.5em}} Y @{}}
	\toprule
	\textbf{Intent} & \textbf{Extracted Entity / Slot} \\
\midrule
	\texttt{get\_prep\_time} & \texttt{recipe\_name} \\
	\texttt{list\_by\_tag} & \texttt{tag} \\
	\texttt{retrieve\_ingredients} & \texttt{recipe\_name} \\
	\texttt{find\_recipe} & \texttt{recipe\_name} \\
	\texttt{list\_by\_ingredient} & \texttt{ingredient} \\
	\texttt{list\_by\_time} & \texttt{minutes} \\
\bottomrule
\end{tabularx}
\end{table}

\subsection{SPAQRQL Queries}

\noindent
The valid entities extracted from the user query are then used to build SPARQL queries to retrieve the desired information from the knowledge graph. Each intent has its own SPARQL query template that is filled with the extracted entities. The following graphs shows the mapping of intents to their corresponding SPARQL query functions:

(IMAGENS DOS QUERIES)

\subsection{Retrieval Augmented Generation}
\noindent
As the descriptions of the recipes in the knowledge graph are more textual and less structured,a RAG (ENCOMENDA REAL) (retrieval augmented generation) system was implemented to be able to retrieve the most relevant descriptions based on the user query.

\subsection{LLM Contextual Response}
\noindent
Finally, the retrieved information from the SPARQL queries and the RAG system is used to generate a summary response to the user query. This is done using an LLM that takes as input the user query, the retrieved recipes, and their descriptions. The LLM then generates a coherent and informative response that answers the user's query based on the retrieved information and past conversation history.

\section{implementation}
\noindent
This section will explain in detail how each of the components described in the approach section were implemented.
\subsection{Knowledge Graph Construction}
\noindent
After pre-processing the dataset the next step is to generate a knowledge graph in RDFS format. The script built for this task uses the RDFlib library (ENCOMENDA REAL) and converts the CSV into an RDF/Turtle knowledge graph by creating cleaned URIs and structured entities for recipes, ingredients, tags, steps, and nutrition, then serializes the result to a .ttl file.
This is an example of how a recipe is represented in the knowledge graph:

% Use fancy verbatim with line breaking to avoid column overflow
\begin{Verbatim}[breaklines=true, breakanywhere=true, fontsize=\small]

@prefix ex: <...> .
@prefix rdf: <...> .
@prefix rdfs: <...> .
@prefix xsd: <...> .

ex:Ingredient a rdfs:Class .

ex:Nutrition a rdfs:Class .

ex:Recipe a rdfs:Class .

ex:Step a rdfs:Class .

ex:Tag a rdfs:Class .

ex:Recipe_100002 a ex:Recipe ;
    rdfs:label "chips ahoy chocolate pie" ;
    ex:hasIngredient ex:Ingredient_butter,
        ex:Ingredient_chocolate_
        chip_cookies,
    ex:hasNutrition ex:Recipe_
    100002_Nutrition ;
    ex:hasStep ex:Recipe_100002
    _StepsSeq ;
    ex:hasTag ex:Tag_60_minutes
    _or_less,
        ex:Tag_course,
    ex:id 100002 ;
    ex:minutes 40 ;
    ex:n_ingredients 5 ;
    ex:n_steps 10 ;
    ex:origin "original randomized" .


ex:Recipe_100002_Nutrition a ex:Nutrition ;
    ex:calories 3.581e+02 ;
    ex:protein 7e+00 ;
    ex:saturatedFat 6.3e+01 ;
    ex:sodium 1.5e+01 ;
    ex:sugar 4.2e+01 ;
    ex:totalFat 3.5e+01 ;
    ex:unknown 1.1e+01 .

ex:Recipe_100002_Step_1 a ex:Step ;
    rdfs:comment "crush 16 cookies in a sealable plastic bag until fine crumbs form" .

ex:Recipe_100002_Step_2 a ex:Step ;
    rdfs:comment "mix 1 1 / 2 cups of the crumbs with melted butter" .

ex:Recipe_100002_StepsSeq a rdf:Seq ;
    rdf:_1 ex:Recipe_100002_Step_1 ;
    rdf:_2 ex:Recipe_100002_Step_2 ;

ex:Ingredient_chocolate_chip_cookies a ex:Ingredient ;
    rdfs:label "chocolate chip cookies" .

ex:Tag_60_minutes_or_less a ex:Tag ;
    rdfs:label "60-minutes-or-less" .

ex:Tag_course a ex:Tag ;
  rdfs:label "course" .


\end{Verbatim}

\noindent
As seen from the example above, each recipe is represented as an instance of the \texttt{Recipe} class, with properties linking to the necessary components. Some of these components have their own classes, as shown below: \\









\begin{table}[H]
\centering
\caption{Recipe class attributes \& RDFS classes.}
\label{tab:rdf-classes}
\begin{tabularx}{\columnwidth}{@{} l @{\hspace{10em}} Y @{}}
\toprule
\textbf{Recipe Attributes} & \textbf{RDFS Classes} \\
\midrule
\texttt{recipe} &  \texttt{Recipe} \\
\texttt{ingredient} & \texttt{Ingredient} \\
\texttt{tag} & \texttt{Tag} \\
\texttt{step} & \texttt{Step} \\
\texttt{nutrition} & \texttt{Nutrition} \\
\texttt{id} & \\
\texttt{minutes} & \\
\texttt{n\_ingredients} & \\
\texttt{n\_steps} & \\

\bottomrule
\end{tabularx}
\end{table}




\noindent
\\
This structured representation allows for the reuse of entities like ingredients and tags across multiple recipes without duplication. The steps were transformed into a RDF sequence (rdf:Seq) as shown in the example above. To maintain their order, each step is linked to the recipe using the \texttt{ex:hasStep} property. Nutrition attributes (calories, protein, saturated fat etc) are encapsulated in a separate \texttt{Nutrition} instance linked to the recipe via the \texttt{ex:hasNutrition} property. Tags are linked using the \texttt{ex:hasTag} property, allowing for categorization and filtering of recipes based on various user tags.
This allows for efficient querying and easier retrieval of the relevant information for the predicted intent.



A small subset with two recipes and their full atribute was put into a ttl tag for observation using the pyvis library after parsing,(the snippet above is taken from this example) This is shown in the figure below:  

(IMAGEM DO GRAFO)

\subsection{Intent Training}
\noindent
The first step to analyse the user's query is to detect its intent. In order to do so an intent classifier was trained using a multilingual BERT model (ENCOMENDA REAL) and the LLM generated dataset mentioned in the section above. 



\noindent
\\
 Using this dataset, we trained a multilingual BERT (ENCOMENDA REAL) intent classifier: it loads the labeled CSV, tokenizes the text with \texttt{bert-base-multilingual-cased}, builds label mappings, fine-tunes a sequence-classification model, evaluates it on an 80/20 split, and finally saves the trained model, tokenizer, and label map for later use in the pipeline.
 
 
\subsection{Entity Extraction}
 

 \noindent
\\
 Alongside the classifier, we developed an entity extractor that uses spaCy (ENCOMENDA REAL) for NER (name entity recognition) and RapidFuzz(ENCOMENDA REAL) for fuzzy matching against the Knowledge Graph entities. As mentioned before, this allows for the extraction of relevant ingredients, tags, recipe names, and time constraints from the user query based on the predicted intent.


The \texttt{list\_by\_time} intent required special attention because user descriptions of preparation time are highly variable and often ambiguous. Unlike other intents that rely on explicit lexical markers, time expressions can appear in many different forms: numerical values (``30 minutos''), written numbers (``trinta minutos''), comparative forms (``menos de 20 minutos'', ``under 10 minutes''), vague descriptions (``algo rápido'', ``something quick''), and mixed-unit expressions such as ``1h20'' or ``1 hora e meia''. In addition, user queries may omit units, combine formats, or express constraints indirectly, which makes it difficult to rely on a single pattern or keyword. The system therefore has to distinguish between exact-time queries (asking for recipes that take exactly a specific number of minutes) and maximum-time queries (asking for recipes that take at most a certain amount of time). It also must interpret comparative structures correctly and normalize written forms into numeric values. For these reasons, the \texttt{list\_by\_time} logic integrates layered numeric parsing, distinction between exact and maximum constraints, translation normalization, and fallback mechanisms so that the fuzzy tag search can be used when no explicit time is detected.


\begin{itemize}

  \item Language detection and normalization:
    \begin{itemize}
      \item Detects query language (PT/EN) using the fasttext model (ENCOMENDA REAL) after detection using Portuguese heuristics (accents, “ç” and common PT words);
      \item Strips accents for robust matching while preserving original forms for display.
    \end{itemize}

  \item spaCy pipeline:
    \begin{itemize}
      \item Loads the (PT/EN) spaCy models;
      \item Adds a flag for keywords related to time (min/minutos/hora/hours);
      \item Extracts noun chunks, preserving order.
    \end{itemize}

  \item Cooking-time parsing in the \texttt{list\_by\_time} intent (PT/EN, tolerant to typos):
    \begin{itemize}
      \item Normalizes number words (e.g., ``quarenta e cinco'', ``forty-five'') into digits;
      \item Uses regexes to detect minutes, hours, or combined ``H+M'' formats for the minutes component (e.g., ``1h 30m'' to "90");
      \item Detects range prefixes (e.g., ``under'', ``menos de'', ``até'') to separate exact vs.\ max\_minutes.
    \end{itemize}
  \item Translation with protected culinary terms:
    \begin{itemize}
      \item Uses Argos Translate offline (ENCOMENDA REAL) for PT$\leftrightarrow$EN, 
      \item A dual pass is used for objective results no matter the input language, since the KG is billingual but has more EN labels;     
      \item Preserves protected PT terms (e.g., ``açorda'', ``brás'') by not translating them.
    \end{itemize}

  \item Candidate tokenization (\texttt{content\_tokens}):
    \begin{itemize}
      \item Removes stopwords and domain noise; sorts tokens for canonical matching;
      \item Aggregation of tokens from original chunks, full text, and its translation.
    \end{itemize}

  \item Knowledge Graph indexing:
    \begin{itemize}
      \item Loads the KG labels into rdflib (ENCOMENDA REAL);
      \item Precomputes accent-stripped normalized labels for fast fuzzy search.
    \end{itemize}

  \item Intent-aware fuzzy search and linking ():
    \begin{itemize}
      \item RapidFuzz (ENCOMENDA REAL) over normalized labels with thresholds:
        \texttt{ingredient}=85, \texttt{tag}=90, \texttt{recipe\_name}=70.
      \item Portuguese labels are preferred with an increment of 3 percentual points when the query appears to be PT (this way portuguese labels are favored).
    \end{itemize}
\end{itemize}

After this the retrieved information is used to generate different SPARQL queries for each intent.These are explained in the following section: 

\subsection{SPARQL Queries}




\subsection{Pipeline}


This file detects language, original and translated query, and detected slots before SPARQL execution.
After, it runs the sequential pipeline and prints per-intent SPARQL outputs; returns a structured dict containing \texttt{intent}, \texttt{confidence}, \texttt{text}, \texttt{slots}, and \texttt{kg\_results}.
\begin{itemize}

  \item Sequential query execution (\texttt{handle\_query}):
    \begin{itemize}
      \item Determines the user intent and extracts slots with \texttt{extract\_and\_link}, then routes execution to the appropriate SPARQL query based on the intent.

      \item list\_by\_ingredient:
        \begin{itemize}
          \item Collects all ingredient labels and runs \texttt{query\_list\_by\_ingredient} for each.
        \end{itemize}

      \item list\_by\_tag:
        \begin{itemize}
          \item Uses all extracted tag labels, executes \texttt{query\_list\_by\_tag}, and deduplicates recipes by URI.
        \end{itemize}

      \item find\_recipe:
        \begin{itemize}
          \item For each candidate recipe name, calls \texttt{query\_find\_recipe} and prints a full recipe card.
        \end{itemize}

      \item retrieve\_ingredients:
        \begin{itemize}
          \item Calls \texttt{query\_retrieve\_ingredients} for each recipe name and prints the ingredient list.
        \end{itemize}

      \item get\_prep\_time:
        \begin{itemize}
          \item For each recipe name, runs \texttt{query\_get\_prep\_time} and prints the preparation time in minutes.
        \end{itemize}

      \item list\_by\_time:
        \begin{itemize}
          \item Reads \texttt{cooking\_time} (exact) or \texttt{max\_minutes} (range) from the slots.
          \item Range queries use \texttt{query\_by\_max\_minutes}; exact queries use \texttt{query\_by\_exact\_minutes}; sets \texttt{result\_basis}.
          \item If no results or no numbers are detected, performs a fuzzy tag fallback:
                injects time-related tags (\texttt{15/30/60-minutes-or-less}), queries \texttt{query\_list\_by\_tag}, deduplicates, and sets the result to \texttt{fuzzy\_tag\_fallback}.
        \end{itemize}

    \end{itemize}
\end{itemize}


\section{RAG implementation}


As mentioned before, this module builds and serves a vector-based semantic search index over the more textual recipe descriptions. Before indexing, the code applies a quality-filtering step as short, boilerplate, or low-information descriptions are detected using heuristics such as length, alphabetic-character ratio, presence of boilerplate patterns (e.g., \verb|prep time|, \verb|yield|), and token count. Any description that fails these checks is skipped. Valid descriptions are then turned into “chunks”. 
In this version, each description is a single chunk which is stored in a list of records containing the fields \verb|{"id", "name", "chunk_id", "text"}|.
The build step encodes all chunks using a SentenceTransformer (ENCOMENDA REAL) mode. The resulting embeddings are L2-normalized and saved to \verb|embeddings.npy|. A FAISS inner-product index is then created and written to \verb|faiss.index|. Alongside it, a metadata file is generated so it can be used later for querying. This whole prosses only needs to be run once, and the resulting files are stored for later queries.
When querying, the index and metadata are lazily loaded and cached. A query string is encoded using the same model and the FAISS index retrieves the most similar chunks. Extra candidates are fetched and deduplication is employed by their \verb|chunk_id|. Results below a similarity threshold are filtered out. The returned list includes the recipe ID, recipe name, chunk ID, the original description text, and the similarity score. 




\section{Decision of LLM summarization module and implementation}


Initial tests with the local Ollama (ENCOMENDA REAL) model proved unsatisfactory. Despite the advantage of running locally without relying on an external service, the models demonstrated semantic inconsistencies with a more critical issue being the amount of time required for responses for each query. Consequently, the implementation was migrated to the Groq API (ENCOMENDA REAL), which provided, with the use of significantly bigger models, improved response accuracy, coherence across turns, and overall stability in the time it took to process and generate the expected output. The Groq API is initialized lazily, ensuring that the module is loaded only when required.
The file that implements this module does the following steps:
\begin{itemize}
    \item conditional loading of environment variables;
    \item lazy loading of the semantic pipeline (ensuring it's only initialized once and resused);
    \item integration with the RAG similarity engine;
    \item prompt construction with history and response policy;
    \item sending requests to Groq via \verb|client.chat.completions|;
    \item logging conversation history in \verb|context.txt|;
    \item maintaining a running session.
\end{itemize}




\subsection{Frontend details}



The frontend of the recipe chatbot was designed with several key user-focused features. Users can create a new chat, which clears the message history stored in the Groq prompt, ensuring that subsequent queries start with a fresh context. While the model is retrieving the context and generating the response, the interface allows users to view the previous message history for reference. The displayed text on the interface can also be dynamically translated. Additionally, all LLM responses are converted to Markdown for rendering, enabling proper formatting of tables and structured content. These frontend features are integrated with the backend described earlier, which is exposed via a FastAPI server (ENCOMENDA REAL). The API has endpoints for health checks, submitting chat queries, and clearing the conversation context. The frontend itself is implemented in React, with a dedicated \texttt{fonts} and \texttt{components} folder to manage the styling and reusable UI elements.




\section{Problems \& Solutions}

\subsection{Deduplication of results}
\noindent
\\
One challenge encountered during the development of the pipeline was the handling of duplicate recipe results arising from overlapping filtering criteria.
The knowledge graph’s structure and tagging system make it possible for a single recipe to satisfy multiple filtering criteria at once—for example, a dish might be labeled both as “quick” and “under 30 minutes,” or appear in multiple result sets inferred through different SPARQL paths. Without explicit handling, such overlaps would cause duplicated recipe entries in the output, misleading users and complicating evaluation of recall and precision. To solve this issue, the pipeline applies a deterministic de-duplication pass (as mentioned above) after SPARQL results are collected. It iterates over the returned recipes, tracks seen URIs in a set, and retains only the first occurrence of each unique recipe. This lightweight but effective mechanism guarantees that results remain clean, non-redundant, and semantically meaningful.


\subsection{Translation normalization}
\noindent
\\
In the early versions, the pipeline relied on a plain large-language-model translation step to normalize user queries into english before intent classification and entity extraction. While this worked reasonably well, it produced non-deterministic translations as the same portuguese phrasing translated into english would not necessarily translate back into the same portuguese string, and small paraphrases in the input would sometimes yield semantically equivalent but structurally different english or portuguese renderings. This inconsistency became problematic because the the spaCy entity extractor depends on predictable lexical patterns. To address this normalization measures were introduced to make the translation stage behave more objectively. 

\subsection{Language and Number normalization}
\noindent
\\
This began by removing stopwords that added noise and led the LLM to produce stylistic rather than literal translations. Secondly, instead of translating token by token, which caused the LLM to reinterpret fragments, the entire sentence was trasnlate as a single unit. Afterwards, we fixed a systematic discrepancy in number handling as PT\textrightarrow{}EN translation tends to convert written numbers into digits (e.g., ``trinta minutos'' \textrightarrow{} ``30 minutes''), while EN\textrightarrow{}PT does not. To preserve semantic symmetry, a digit-to-word mapping that rewrites numbers consistently in the canonical representation was introduced. Finally, we stored the original query, the detected language, and the normalized English form so downstream components always receive a stable and deterministic string for tokenization. Together, these fixes substantially reduced variation and improved the intent classification's reliability.

\subsection{Pre-loading of heavy components}
\noindent
\\
Another crucial architectural decision in the pipeline is the pre-loading of the heavy components, namely the multilingual spaCy pipeline and the knowledge-graph, before any user query is processed. The spaCy model initialization involves loading the statistical components, taggers, dependency parsers, custom extension attributes and multilingual vocabulary vectors, as performing this at runtime for every query would be expensive. Similarly, the knowledge graph, sourced from a Turtle file exceeding one million lines, took too long to be reparsed from RDF on demand (33s for each query). To address this, the pipeline implements a PKL-based (ECOMENDA REAL) caching system: on the first run, the TTL file is parsed into an in-memory graph structure, which is then serialized to disk as a .pkl snapshot. Subsequent executions detect the presence of the pickle file and load the graph directly and only once at the start in a fraction of the time. This strategy not only improves performance dramatically but also stabilizes memory usage, since the expensive graph structure is built exactly once and then reused.


\noindent
\\
The pipeline also introduces a dedicated mechanism that controls when entity extraction is executed, preventing unnecessary or harmful parsing for intents that do not require semantic grounding. For queries such as greetings, confirmations, or generic help requests, attempting entity extraction would lead to inconsistent results, noise in the entity linker, and contradictions between the detected intent and the returned slot set.




\subsection*{Conversation History of Recent Messages}

To avoid exceeding the per-minute request limit and reduce computational costs, a compact history of the last three messages was implemented. While the full context of all the results in the entity extraction is given to groq for the current message, for the messages in the history only two fields are stored:

\begin{itemize}
    \item \verb|query| – the original user question,
    \item \verb|response| – the final response returned by the LLM.
\end{itemize}

\noindent
\\
This history is used to generate a reduced context block appended at the end of the \textit{prompt}. The messages appear in order of recency, with the most recent first. The number of messages included is configurable (default: 3). Placing the history at the end ensures that the trimming system always cuts from the tail, preserving the most relevant part of the main prompt.

\noindent
\\
As this implementation made the prompts longer, it was also necessary to add a trimming mechanism to the current query. This is done by beginning with the items found in the fuzzy matching with the lowest confidences. Then, for each of these items, the SPARQL results are eliminated one by one until the threshold of 7000 tokens is reached. This was the value decide because the model used for the experiments (chatgpt 120b) (ENCOMENDA REAL) has a per minute prompt limit of 8000. 


\subsection{Groq base prompt}

Before each query is sent to Groq, a structured base prompt is provided that defines the model’s role, constraints, and formatting rules. This prompt positions the model as a bilingual PT/EN recipe assistant and explicitly instructs it to always answer in the same language as the user’s query—including translating tags, ingredients, steps, and recipe names when necessary. It also clarifies what contextual information the model is strictly allowed to use: the current query, up to three previous conversational messages, and any structured data extracted from the knowledge graph. Equally important, the prompt imposes strict behavioral guidelines: the model must rely solely on the provided structured data, avoid hallucinating missing information, and follow a modular answer format when multiple entities are detected (ingredients, tags, name). It also specifies how the model must handle meta-queries such as “what did I ask before?” by checking the last few conversational messages and replying strictly based on that context, if relevant. For off-topic queries, the prompt instructs the model not to fabricate recipe results and instead provide a short clarification request or a single safe example query that can guide the user back to a valid intent. The final section of the prompt firstly enforces a conservative use of semantic description snippets (RAG), allowing them only when they are clearly relevant and never as a source for inventing missing structured data. Finally it also defines a discovery footer containing any secondary results that were not elaborated on in the main response. This prompt was built through iterative testing and refinement to ensure consistency and alignment with user expectations. After this base prompt, a structured block is appended containing:
\begin{itemize}
    \item The pipeline result (intent, confidence, slots, SPARQL results);
    \item The top similarity snippets from the RAG index;
    \item The compact block of recent message history.
\end{itemize}




% -------- EXPERIMENTATION ----------
\section{Experimentation}

\subsection{Setup}
We evaluated PT/EN queries by ingredients, tags, and time constraints.

\subsection{Results}
Positive cases showed accurate intent routing and bilingual output. Initial failures in time parsing and API configuration were mitigated through relaxed matching and environment validation.

Resultados

- Mostrar o quão boas as SPARQL queries foram a buscar os resultados desejados do dataset - resultado

- Refletir a introdução do RAG para a descrição no sistema, mesmo com as melhorias. - resultado

- Mostrar a interpretação das mensagens do historico + prompt + resultados do groq (receitas/ingredients/tags/steps/time pedidos de acordo com a query) - uma tablea com exeplos na parte do prompt engineering



\subsection{Metrics}
\begin{itemize}
  \item Intent accuracy (placeholder);
  \item Entity linking precision@k (placeholder);
  \item Time extraction success rate (placeholder);
  \item Human quality judgments for bilingual summaries (placeholder).
\end{itemize}

% -------- CONCLUSIONS ----------
\section{Conclusions and Future Work}

We demonstrated that combining symbolic reasoning with neural generation produces accurate and fluent responses. The bilingual KG-powered assistant effectively supports multiple query types.

Future work includes:
\begin{itemize}
  \item expanding KG coverage;
  \item adding automatic evaluation metrics;
  \item improving translation robustness;
  \item integrating structured outputs.
\end{itemize}

% -------- REFERENCES ----------

\footnotesize \bibliographystyle{IEEEtran}
\bibliography{report}

\end{document}
